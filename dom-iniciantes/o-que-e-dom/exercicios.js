// URL da pagina utilizando objeto window
const hrefPagina= window.location.href;
console.log(hrefPagina)

// Primeiro elemento da pagina com classe ativo
const elementoAtivo = document.querySelector('.ativo'); // identifica o primeiro elemento com classe ativo


// Linguagem do navegador (objeto maximo, window)
const language = window.navigator.language
console.log(language)

// Largura da Janela
const width = window.innerWidth
console.log(width)